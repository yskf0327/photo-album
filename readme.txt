【やることリスト】

1. albumData配列を使って、メイン画像エリアに最初の画像とキャプション、日付を表示する
(index_sample.htmlを参考にする。日付はDateクラスを利用する。)

2. albumData配列を使って、サムネイル画像エリアに画像を表示する
(index_sample.htmlを参考にする。配列データを扱うのでfor文で作成する。)

3. サムネイル画像をクリックしたら、メイン画像エリアの画像とキャプション、日付が適切なデータに切り替わるようにする
(複数の要素にaddEventListenerするにはどうすれば良いのか？サムネイル画像についているdata-index属性の値を上手く使う)。

4. 最初のサムネイル画像にis-activeクラスつける。

5. is-activeクラスをクリックしたサムネイル画像に付け替える